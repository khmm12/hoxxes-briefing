// The app's canonical public address. Shared links point here regardless of the
// host actually serving the page (preview deploy, LAN, localhost), so a briefing
// shared from anywhere still opens the real app.
export const canonicalUrl = 'https://hoxxes-briefing.vercel.app'
